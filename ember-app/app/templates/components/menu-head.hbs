<div class="top-nav">
    <a class="sidebar-toggle visible-sm visible-xs" >
        <i class="icon-hamburger icon"></i>
    </a>

    <div class="section-content ">
        <div class="app-header flex-grow-1 flex">
            {{#if backLinkTitle}}
                <div class="flex">
                    {{#link-to backLinkUri class='back-link'}}
                        <i class="icon icon-back"></i>
                        <span class="back-context">{{backLinkTitle}}</span>
                    {{/link-to}}
                </div>
            {{/if}}

            <div class="app-name-container flex-grow-1">
                <span class="app-name">{{title}}</span>
            </div>

        </div>

    </div>

</div>